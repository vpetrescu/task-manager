import setuptools

setuptools.setup(
    name="taskmanager",
    version="1.0",
    author="vpetrescu",
    description="Task manager library",
    include_package_data=True,
    packages=setuptools.find_packages()
)